import { defineConfig } from 'astro/config';

export default defineConfig({
  output: 'static',
  // site: 'https://angelgm876.github.io', // Actualiza esto con tu dominio de Amplify si lo tienes
});
